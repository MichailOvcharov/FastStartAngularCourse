import { ChangeBorderDirectiveDirective } from './change-border-directive.directive';

describe('ChangeBorderDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeBorderDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
